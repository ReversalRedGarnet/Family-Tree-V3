# Installed by npm — restored with `npm install`, never committed.
node_modules/

# Build output — regenerated by `npm run build`.
dist/

# Local env and editor noise.
.env
.env.local
.DS_Store
*.log
